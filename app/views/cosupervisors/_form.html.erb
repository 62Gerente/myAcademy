<%= form_for(@cosupervisor) do |f| %>
  <% if @cosupervisor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cosupervisor.errors.count, "error") %> prohibited this cosupervisor from being saved:</h2>

      <ul>
      <% @co_supervisor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :institution %><br>
    <%= f.collection_select :institution_id, Institution.all, :id, :name %>
  </div>
  </div>
  <div class="field">
    <%= f.label :teacher %><br>
    <%= f.collection_select :teacher_id, Teacher.all, :id, :name %>
  </div>
  </div>
  <div class="field">
    <%= f.label :thesis_supervision %><br>
    <%= f.collection_select :thesis_supervision_id, ThesisSupervision.all, :id, :title %>
  </div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
