<%= form_for(@thesis_supervision) do |f| %>
  <% if @thesis_supervision.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thesis_supervision.errors.count, "error") %> prohibited this thesis_supervision from being saved:</h2>

      <ul>
      <% @thesis_supervision.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :b_date %><br>
    <%= f.date_select :b_date %>
  </div>
  <div class="field">
    <%= f.label :e_date %><br>
    <%= f.datetime_select :e_date %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :teacher %><br>
    <%= f.collection_select :teacher_id, Teacher.all, :id, :name %>
  </div>

  <div class="field">
    <%= f.label :thesis %><br>
    <%= f.collection_select :thesis_id, Thesis.all, :id, :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
